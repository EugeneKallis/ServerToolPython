apiVersion: v1
kind: Service
metadata:
  name: {{ include "server-tool-python.fullname" . }}
  labels:
    {{- include "server-tool-python.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "server-tool-python.selectorLabels" . | nindent 4 }}
