FROM node:20-alpine

WORKDIR /app

# Install dependencies based on the preferred package manager
COPY package.json package-lock.json* ./
RUN npm install

# The source code will be mounted as a volume
# CMD will be overridden in docker-compose.yml if needed

CMD ["npm", "run", "dev"]
